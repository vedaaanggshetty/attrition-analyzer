package com.example.AuthService.entity;

/**
 * Authenticated roles known to the system.
 *
 * Guest is intentionally not represented here - it is the absence of
 * authentication, not a stored credential record.
 */
public enum Role {
    HR
}
